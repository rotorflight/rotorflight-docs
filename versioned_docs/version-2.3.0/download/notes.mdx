---
sidebar_position: 1
---

# v2.3.0 Upgrade notes

## ⚠️ Upgrading to Rotorflight 2.3.0 using `diff`

After applying your `diff` in the CLI, check the following settings — their [defaults have changed](https://github.com/rotorflight/rotorflight-firmware/blob/release/4.6.0/Changes.md#defaults):

* **Rate type** — If you're using Actual Rates, type `set rates_type = ACTUAL`. The default is now `ROTORFLIGHT`.
* **Motor poles** — If you're using an 8 pole motor, type `set motor_poles = 8,0,0,0`. The default is now 0 poles, so users have to specify it.
* **Rescue flip** — If you don't want auto-flip to upright, run `set rescue_flip = OFF`. The default is now `ON`.
* **Rotorflight Governor** — Type `set gov_mode = ELECTRIC` to configure an electric motor if you were using the Rotorflight governor.

As always, do not blindly apply your old configuration. Go through all settings carefully before your first flight.
